# Palabras reservadas

# Estas no las podemos usar como nombres de variables

"""
    - if
    - else
    - elif
    - in
    - with
    - for
    - while
    - or
    - and
    - continue
    - break
    - ...
"""

# if = "una variable invalida :("  # Esto nos da error
es_verdad = "esto es verdadero"

# Bloques de código
if True: # Si esto se cumple se ejecutara el código que está "dentro"
    print("Esto siempre es verdad :D") # El bloque se identifica con la identación que puede ser una tabulación o 4 espacios
